<%
  raise "_index_pagination partial not found. Please create an _index_pagination.html.erb under your app/views/manageable folder
  with the desired pagination output. You can call the provided 'manageable_pagination' helper like this (remember to adapt it to your
  pagination library:

  manageable_pagination :current_page => collection.current_page, :num_pages => collection.num_pages
  "
%>